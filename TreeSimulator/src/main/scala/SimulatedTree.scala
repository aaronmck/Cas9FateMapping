package main.scala

/**
 * The idea: draw from a Dirichlet distribution (parametrized by the normalized count data) the next node.  It's heirarchical --
 * we have one Dd to draw the next site to edit, then another for the per-site modification
 */
class SimulatedTree {
  // for each event, find the most likely previous event given the inverse probabilities of the current edits.


  //
}

object SimulatedTree {

}
